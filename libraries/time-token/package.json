{
  "name": "@graphene/time-token",
  "version": "1.0.0",
  "description": "Library used to generate a server-side token which may be emailed and then statelessly verified on the server.",
  "keywords": "verify email",
  "main": "index.js",
  "config": {
    "expire_min": 10
  },
  "dependencies": {
    "@graphene/local-secret": "^1.0.0"
  },
  "scripts": {
    "test": "mocha --compilers js:babel-core/register --recursive",
    "test:watch": "npm test -- --watch"
  },
  "author": "jcalfee",
  "license": "BSD-2-Clause-FreeBSD",
  "devDependencies": {
    "assert": "^1.3.0",
    "babel-cli": "^6.2.0",
    "babel-core": "^6.2.0",
    "babel-preset-es2015": "^6.1.18",
    "mocha": "^2.3.4"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  }
}
